% 海塞矩阵
H = [
    4, -2, 0;
    -2, 4, 0;
    0, 0, 2
];
% 一次项系数向量
C = [
    0;
    0;
    1
];
% 线性不等式约束系数矩阵
A = [1, 3, 2];
% 线性不等式约束常数项系数向量
b = [6];
% 线性等式约束系数矩阵
Aeq = [2, -1, 1];
% 线性等式约束常数项系数向量
beq = [4];
% 下界
lb = zeros(3, 1)
% 上界
ub = []


% 调用
[xopt, fopt] = quadprog(H, C, A, b, Aeq, beq, lb, ub)